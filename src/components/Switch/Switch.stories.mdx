import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

import Switch from './Switch.vue';

export const defaultArgs = {
  id: {
    control: {
      type: 'text',
    },
    defaultValue: 'switch',
  },
  value: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
  size: {
    control: {
      type: 'select',
      options: ['small', 'medium', 'large'],
    },
    defaultValue: 'medium',
  },
  disabled: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
  onChange: {
    control: {
      type: 'action',
    },
    action: 'change',
  },
};

<Meta
  title="Components/Switch"
  component={Switch}
  argTypes={{ ...defaultArgs }}
/>

export const Default = (args) => {
  return {
    components: { Switch },
    setup() {
      return {
        args,
      };
    },
    template: `<Switch v-bind="args" />`,
  };
};

# Switch

Switch is a component that allows users to toggle between two states.
It is used to turn something on or off. It is commonly used to activate or deactivate a setting.
It is also used to turn a feature on or off. It is used to enable or disable a feature.
It is used to turn a feature on or off. It is used to enable or disable a feature.

### Overview

<Canvas withSource="open">
  <Story
    name="Overview"
    argTypes={{ ...Default.argTypes }}
    args={{
      value: false,
      size: 'small',
    }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Playground

> Changes you make in the controls will be reflected in the example above.

<ArgsTable story="Overview" exclude={/^(on|change|onChange.*)$/} />

## Stories

### Small

<Canvas withSource="open">
  <Story
    name="Small"
    argTypes={{ ...Default.argTypes }}
    args={{
      value: false,
      size: 'small',
    }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Medium

<Canvas withSource="open">
  <Story
    name="Medium"
    argTypes={{ ...Default.argTypes }}
    args={{
      value: false,
      size: 'medium',
    }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Large

<Canvas withSource="open">
  <Story
    name="Large"
    argTypes={{ ...Default.argTypes }}
    args={{
      value: false,
      size: 'large',
    }}
  >
    {Default.bind({})}
  </Story>
</Canvas>
