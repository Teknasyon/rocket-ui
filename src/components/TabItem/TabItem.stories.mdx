import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import TabItem from './TabItem.vue';

export const Default = (args) => ({
  components: { TabItem },
  setup() {
    return { args, click: action('click') };
  },
  template: `
    <TabItem v-bind="args" @click="click">
      <template #tab={activeTab}>
        <h1>Tab {{activeTab + 1}}</h1>
        <p>Active tab {{activeTab}}</p>
      </template>
    </TabItem>
  `,
});

export const defaultArgs = {
  id: {
    control: {
      type: 'text',
    },
    defaultValue: 'tab-item-1',
  },
  tab: {
    control: {
      type: 'object',
    },
    defaultValue: {
      id: 'tab-1',
      label: 'Tab 1',
      icon: 'home',
      iconKind: 'filled',
      badge: '1',
    },
  },
  variant: {
    control: {
      type: 'select',
      options: [
        'default',
        'pill',
        'underline',
        'icon-only',
        'icon-pill',
        'text-only',
      ],
    },
    defaultValue: 'default',
  },
  size: {
    control: {
      type: 'select',
      options: ['small', 'medium', 'large'],
    },
    defaultValue: 'medium',
  },
  color: {
    control: {
      type: 'color',
    },
    defaultValue: '',
  },
  bgColor: {
    control: {
      type: 'color',
    },
    defaultValue: '',
  },
  activeColor: {
    control: {
      type: 'color',
    },
    defaultValue: '',
  },
  activeBgColor: {
    control: {
      type: 'color',
    },
    defaultValue: '',
  },
  align: {
    control: {
      type: 'select',
      options: ['left', 'center', 'right'],
    },
    defaultValue: 'center',
  },
  activeTab: {
    control: {
      type: 'number',
    },
    defaultValue: 0,
  },
};

<Meta
  title="Components/TabItem"
  component={TabItem}
  argTypes={{ ...defaultArgs }}
  parameters={{
    viewMode: 'docs',
    layout: 'centered',
  }}
/>

# TabItem

TabItem is a component that renders a tab item. It is not meant to be used on its own.

### Overview

<Canvas withSource="open">
  <Story
    name="Overview"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'default' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Playground

> Changes you make in the controls will be reflected in the example above.

<ArgsTable story="Overview" exclude={/^(click|icon|on.*)$/} />

## Stories

### Default

<Canvas withSource="open">
  <Story
    name="Default"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'default' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Icon Only

<Canvas withSource="open">
  <Story
    name="Icon Only"
    argTypes={{ ...Default.argTypes }}
    args={{
      variant: 'icon-only',
      tab: {
        id: 'tab-item-2',
        label: 'Tab 1',
        icon: 'home',
        iconKind: 'filled',
      },
    }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Pill

<Canvas withSource="open">
  <Story
    name="Pill"
    argTypes={{ ...Default.argTypes }}
    args={{
      variant: 'pill',
      tab: {
        id: 'tab-item-3',
        label: 'Tab 1',
        icon: 'home',
        iconKind: 'filled',
      },
    }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Icon Pill

<Canvas withSource="open">
  <Story
    name="Icon Pill"
    argTypes={{ ...Default.argTypes }}
    args={{
      variant: 'icon-pill',
      tab: {
        id: 'tab-item-4',
        label: 'Tab 1',
        icon: 'home',
        iconKind: 'filled',
      },
    }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Text Only

<Canvas withSource="open">
  <Story
    name="Text Only"
    argTypes={{ ...Default.argTypes }}
    args={{
      variant: 'text-only',
      tab: {
        id: 'tab-item-5',
        label: 'Tab 1',
        icon: 'home',
        iconKind: 'filled',
      },
    }}
  >
    {Default.bind({})}
  </Story>
</Canvas>
