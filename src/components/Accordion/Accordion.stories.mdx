import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import Accordion from './Accordion.vue';

export const Default = (args) => ({
  components: { Accordion },
  setup() {
    return { args, update: action('update:modelValue') };
  },
  template: `<Accordion v-bind="args" @update:modelValue="update"/>`,
});

export const defaultArgs = {
  accordions: {
    control: {
      type: 'array',
    },
    defaultValue: [
      {
        title: 'Accordion 1',
        content: 'Accordion 1 content',
      },
    ],
  },
  modelValue: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
};

<Meta
  title="Components/Accordion"
  component={Accordion}
  argTypes={{ ...defaultArgs }}
  parameters={{
    viewMode: 'docs',
  }}
/>

# Alert

The Accordion Component is a UI element that allows the user to show and hide content within an expandable and collapsible section. It consists of a container that holds multiple panels, where each panel has a header and a body. When a panel's header is clicked, the corresponding body is shown or hidden.

- [Overview](#overview)

- [Playground](#playground)

- [Usage](#usage)

- [Stories](#stories)

- [Customizable Slots](#customizable-slots)

- [Tips](#tips)

## Overview <a id="overview" />

The Accordion Component provides a compact way of displaying large amounts of information in a small space. It is particularly useful for displaying hierarchical data, where each panel represents a different level of information. The Accordion Component is also a good way to present frequently asked questions (FAQs) as it makes it easy for users to find the information they are looking for.

<Canvas withSource="open">
  <Story name="Overview" argTypes={{ ...Default.argTypes }} args={{}}>
    {Default.bind({})}
  </Story>
</Canvas>

## Playground <a id="playground" />

> Changes you make in the controls will be reflected in the example above. Try it yourself!

<ArgsTable story="Overview" exclude={/^(default|update.*)$/} />

## Usage <a id="usage" />

### With props

- accordions (Array): The accordions prop is an array that holds the data for each panel in the Accordion Component. Each panel is an object with properties such as header and body.

- modelValue (Boolean): The modelValue prop is a boolean that determines whether the Accordion Component is in an expanded or collapsed state. A value of true indicates that the Accordion Component is expanded, and a value of false indicates that it is collapsed.

## Stories <a id="stories" />

### Single Collapse

<Canvas withSource="open">
  <Story name="Single Collapse" argTypes={{ ...Default.argTypes }} args={{}}>
    {Default.bind({})}
  </Story>
</Canvas>

### Accordion

<Canvas withSource="open">
  <Story
    name="Accordion"
    argTypes={{ ...Default.argTypes }}
    args={{
      accordions: [
        {
          title: 'Accordion 1',
          content: 'Accordion 1 content',
          isExpanded: true,
        },
        {
          title: 'Accordion 2',
          content: 'Accordion 2 content',
        },
        {
          title: 'Accordion 3',
          content: 'Accordion 3 content',
        },
        {
          title: 'Accordion 4',
          content: 'Accordion 4 content',
          isDisabled: true,
        },
      ],
    }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

## Customizable Slots <a id="customizable-slots" />

### Tips <a id="tips" />

💡 Use clear and concise headings for each panel's header to help users quickly understand the content within each panel.

💡 Consider using icons or graphics within the header to make the Accordion Component more visually appealing.

💡 Make sure that the Accordion Component is accessible to users who may be using assistive technologies, such as screen readers.

💡 Use the modelValue prop wisely to control the default state of the Accordion Component. For example, you might want to set the modelValue prop to true for the panel that contains the most important information.

💡 Try to limit the amount of content within each panel to ensure that the Accordion Component remains compact and easy to use.
