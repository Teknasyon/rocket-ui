import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

import Chip from './Chip.vue';

export const Default = (args) => ({
  components: { Chip },
  setup() {
    return { args };
  },
  template: `
    <Chip v-bind="args"/>
  `,
});

export const defaultArgs = {
  variant: {
    control: {
      type: 'select',
      options: [
        'primary',
        'secondary',
        'success',
        'danger',
        'warning',
        'info',
        'light',
        'dark',
      ],
    },
    defaultValue: 'primary',
  },
  size: {
    control: {
      type: 'select',
      options: ['small', 'medium', 'large'],
    },
    defaultValue: 'medium',
  },
  label: {
    control: {
      type: 'text',
    },
    defaultValue: 'Chip',
  },
  iconLeft: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
  disabled: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
  icon: {
    control: {
      type: 'text',
    },
    defaultValue: '',
  },
  square: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
  onClick: {
    control: {
      type: 'function',
    },
    action: 'click',
  },
};

<Meta
  title="Components/Chips"
  component={Chip}
  argTypes={{ ...defaultArgs }}
  parameters={{
    viewMode: 'docs',
    layout: 'centered',
  }}
/>

# Chips

Chip components are used to display small blocks of information.
They are used to represent small entities in a compact way.

They can be used to display information such as tags, contacts, or even files.
They can be used in forms, tables, or as standalone elements.
They can be used as a way to filter content.

### Overview

<Canvas>
  <Story name="Overview" argTypes={{ ...Default.argTypes }} args={{}}>
    {Default.bind({})}
  </Story>
</Canvas>

### Playground

> Changes you make in the controls will be reflected in the example above.

<ArgsTable story="Overview" exclude={/^(click|on.*)$/} />

## Stories

### Primary

<Canvas>
  <Story name="Primary" argTypes={{ ...Default.argTypes }} args={{}}>
    {Default.bind({})}
  </Story>
</Canvas>

### Secondary

<Canvas>
  <Story
    name="Secondary"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'secondary' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Success

<Canvas>
  <Story
    name="Success"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'success' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Danger

<Canvas>
  <Story
    name="Danger"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'danger' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Warning

<Canvas>
  <Story
    name="Warning"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'warning' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Info

<Canvas>
  <Story
    name="Info"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'info' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Light

<Canvas>
  <Story
    name="Light"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'light' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Dark

<Canvas>
  <Story
    name="Dark"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'dark' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>
