import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import Chip from './Chip.vue';

export const Default = (args) => ({
  components: { Chip },
  setup() {
    return {
      args,
      clickChip: action('clickChip'),
      clickIcon: action('clickIcon'),
    };
  },
  template: `
    <Chip v-bind="args" @clickChip="clickChip" @clickIcon="clickIcon"/>
  `,
});

export const defaultArgs = {
  variant: {
    control: {
      type: 'select',
      options: ['primary', 'secondary', 'success', 'error', 'warning', 'info'],
    },
    defaultValue: 'primary',
  },
  size: {
    control: {
      type: 'select',
      options: ['small', 'medium', 'large'],
    },
    defaultValue: 'medium',
  },
  label: {
    control: {
      type: 'text',
    },
    defaultValue: 'Chip',
  },
  disabled: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
  prependIcon: {
    control: {
      type: 'text',
    },
    defaultValue: '',
  },
  appendIcon: {
    control: {
      type: 'text',
    },
    defaultValue: 'close',
  },
  square: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
  ghost: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
};

<Meta
  title="Components/Chips"
  component={Chip}
  argTypes={{ ...defaultArgs }}
  parameters={{
    viewMode: 'docs',
    layout: 'centered',
  }}
/>

# Chips

Chip components are used to display small blocks of information.
They are used to represent small entities in a compact way.

They can be used to display information such as tags, contacts, or even files.
They can be used in forms, tables, or as standalone elements.
They can be used as a way to filter content.

### Overview

<Canvas>
  <Story
    name="Overview"
    argTypes={{ ...Default.argTypes }}
    args={{
      prependIcon: 'face',
    }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Playground

> Changes you make in the controls will be reflected in the example above.

<ArgsTable story="Overview" exclude={/^(click|on.*)$/} />

## Stories

### Primary

<Canvas withSource="open">
  <Story name="Primary" argTypes={{ ...Default.argTypes }} args={{}}>
    {Default.bind({})}
  </Story>
</Canvas>

### Secondary

<Canvas withSource="open">
  <Story
    name="Secondary"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'secondary' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Success

<Canvas withSource="open">
  <Story
    name="Success"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'success' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Error

<Canvas withSource="open">
  <Story
    name="Error"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'error' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Warning

<Canvas withSource="open">
  <Story
    name="Warning"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'warning' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Info

<Canvas withSource="open">
  <Story
    name="Info"
    argTypes={{ ...Default.argTypes }}
    args={{ variant: 'info' }}
  >
    {Default.bind({})}
  </Story>
</Canvas>
