import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import Checkbox from './Checkbox.vue';

export const Default = (args) => {
  return {
    components: { Checkbox },
    setup() {
      return { args, change: action('change') };
    },
    template: `<Checkbox v-bind="args" @change="change"/>`,
  };
};

export const defaultArgs = {
  id: {
    control: {
      type: 'text',
    },
    defaultValue: 'checkbox',
  },
  checked: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
  label: {
    control: {
      type: 'text',
    },
    defaultValue: 'Checkbox',
  },
  right: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
  indeterminate: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
  disabled: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
  required: {
    control: {
      type: 'boolean',
    },
    defaultValue: false,
  },
};

<Meta
  title="Components/Checkbox"
  component={Checkbox}
  argTypes={{ ...defaultArgs }}
  parameters={{
    viewMode: 'docs',
  }}
/>

# Checkbox

Checkbox is a component that allows the user to select one or more options from a set. It can be used to select multiple options in a form.

It can also be used as a single option selector, if you want to present the user with only one option to choose from.

The user can select or deselect the option by clicking on the checkbox.

The user can also select or deselect the option by clicking on the label next to the checkbox.

`*`The user can also select or deselect the option by using the keyboard. The user can use the spacebar to select or deselect the option.

<Canvas withSource="open">
  <Story
    name="Overview"
    argTypes={{ ...Default.argTypes }}
    args={{
      id: 'checkbox-1',
      label: 'Checkbox label',
      right: false,
    }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

### Playground

> Changes you make in the controls will be reflected in the example above.

<ArgsTable story="Overview" />

## Stories

### Default

<Canvas withSource="open">
  <Story
    name="Default"
    argTypes={{ ...Default.argTypes }}
    args={{
      id: 'checkbox-1',
      label: 'Checkbox label',
      right: false,
    }}
  >
    {Default.bind({})}
  </Story>
</Canvas>

###### `*` is mean Not yet implemented
